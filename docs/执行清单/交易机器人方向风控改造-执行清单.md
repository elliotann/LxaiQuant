# 交易机器人做单方向与风控参数改造 - 执行清单

- 创建时间: 2026-06-23
- 最后更新: 2026-06-25
- 优先级说明: P0=必须完成, P1=建议完成

---

## 一、数据库变更（共 2 项）

| 序号 | 任务 | 脚本路径 | 优先级 | 状态 |
|------|------|---------|--------|------|
| 1 | TradingBot 表新增 `direction` 字段 | `docs/db/alter_trading_bot_add_direction.sql` | P0 | 已完成 |
| 2 | 创建 `bot_parameter` 表（value 列使用 json 类型） | `docs/db/create_bot_parameter_table.sql` | P0 | 已完成 |

## 二、后端新增（共 5 项）

| 序号 | 任务 | 模块 | 文件 | 优先级 | 状态 |
|------|------|------|------|--------|------|
| 3 | BotParameter DO 实体 | ai-strategy | `entity/dos/BotParameter.java` | P0 | 已完成 |
| 4 | BotParameterMapper + XML | ai-strategy | `mapper/BotParameterMapper.java` + `.xml` | P0 | 已完成 |
| 5 | IBotParameterService 接口 | ai-strategy | `service/IBotParameterService.java` | P0 | 已完成 |
| 6 | BotParameterServiceImpl 实现 | ai-strategy | `service/impl/BotParameterServiceImpl.java` | P0 | 已完成 |
| 7 | 参数管理前端API + 业务逻辑 | ai-quant | `controller/TradingBotController.java` 新增接口 | P0 | 已完成 |

## 三、后端改造（共 5 项）

| 序号 | 任务 | 模块 | 文件 | 优先级 | 状态 |
|------|------|------|------|--------|------|
| 8 | TradingBot DO 新增 direction 字段 | ai-strategy | `entity/dos/TradingBot.java` | P2 | 已完成 |
| 9 | TradingBotMapper.xml 映射更新 | ai-strategy | `mapper/TradingBotMapper.xml` | P2 | 已完成 |
| 10 | TradingBotServiceImpl 改造（参数业务集成） | ai-strategy | `service/impl/TradingBotServiceImpl.java` | P2 | 已完成 |
| 11 | LiveTradingServiceImpl 引擎风控改造（切到bot_parameter，三层兼容：新JSON记录→旧展开记录→旧exit_rules_config） | ai-quant | `service/impl/LiveTradingServiceImpl.java` | P2 | 待开始 |
| 12 | DefaultDealStrategyTrade 方向过滤 | ai-engine | `core/impl/DefaultDealStrategyTrade.java` | P2 | 待开始 |

## 四、前端改造（共 7 项）

| 序号 | 任务 | 项目 | 文件 | 优先级 | 状态 |
|------|------|------|------|--------|------|
| 13 | PC robot.ts 新增 bot_parameter API | PC前端 | `src/api/robot.ts` | P1 | 已完成 |
| 14 | PC EditTradingBot.vue 方向选择器 | PC前端 | `src/components/trading/EditTradingBot.vue` | P1 | 已完成 |
| 15 | PC EditTradingBot.vue 风控参数改走 bot_parameter API（三组 JSON 保存 + 三层兼容加载） | PC前端 | `src/components/trading/EditTradingBot.vue` | P1 | 已完成 |
| 16 | PC RiskControlForm.vue 双模式渲染改造（支持 cards/inline 布局） | PC前端 | `src/components/trading/RiskControlForm.vue` | P1 | 已完成 |
| 17 | PC RiskControlForm.vue 修复编译/运行时错误（标签平衡、v-model、smcExit缺失） | PC前端 | `src/components/trading/RiskControlForm.vue` | P1 | 已完成 |
| 18 | PC TradingBots.vue 列表展示 direction 字段 | PC前端 | `src/views/trading/TradingBots.vue` | P1 | 待开始 |
| 19 | H5 BotFromIndicator.vue 风控参数改走 bot_parameter API | H5前端 | `src/views/trading/BotFromIndicator.vue` | P1 | 待开始 |

## 五、测试（共 2 项）

| 序号 | 任务 | 优先级 | 状态 |
|------|------|--------|------|
| 20 | 方向过滤逻辑验证 | P1 | 待开始 |
| 21 | 风控参数优先级验证 | P1 | 待开始 |

## 六、数据库脚本

### alter_trading_bot_add_direction.sql

```sql
ALTER TABLE `trading_bot`
  ADD COLUMN `direction` VARCHAR(16) NOT NULL DEFAULT 'BOTH' COMMENT '做单方向: LONG-只做多, SHORT-只做空, BOTH-双向',
  ADD INDEX `idx_direction` (`direction`);
```

### create_bot_parameter_table.sql

```sql
CREATE TABLE `bot_parameter` (
  `id`          bigint       AUTO_INCREMENT,
  `bot_id`      varchar(32)  NOT NULL COMMENT '机器人ID',
  `group_name`  varchar(64)  NOT NULL COMMENT '分组名：config/positionRisk/exitRules',
  `name`        varchar(128) NOT NULL COMMENT '参数名（与group_name同名，存单条完整JSON）',
  `value`       json         DEFAULT NULL COMMENT '参数值（完整JSON对象）',
  `created_at`  datetime     DEFAULT CURRENT_TIMESTAMP,
  `updated_at`  datetime     DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_bot_param` (`bot_id`, `group_name`, `name`),
  KEY `idx_bot_group` (`bot_id`, `group_name`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='机器人参数表';
```
