# Lenzito 后端重构 — 执行清单

> 与 [Lenzito-后端重构计划.md](./Lenzito-后端重构计划.md) 配套使用。  
> **规则**：每步完成后打勾 `[x]`，执行 `mvn clean compile -DskipTests`（或全量 test），通过后再做下一步。

**当前阶段**：阶段 0  
**建议分支**：`refactor/backend-phase0`（由你本地创建）

**架构约定（v1.1）**：

- 可启动应用：**仅** `ai-quant`、`ai-task`
- **`ai-order`**：纯库模块，**不**保留 `AiOrderApplication`
- **`ai-ml`**：新建库模块，从 `ai-quant` 迁出 ML/因子挖掘代码

---

## 进度总览

| 阶段 | 名称 | 状态 |
|------|------|------|
| 0 | 基线与护栏 + order 去启动化 | 🔄 进行中 |
| 1 | 边界清晰化 + **新建 ai-ml** | ⏳ 待开始 |
| 2 | God Class 拆分 | ⏳ 待开始 |
| 3 | 测试与可观测性 | ⏳ 待开始 |
| 4 | 包迁移（可选） | ⏳ 待开始 |

---

## 阶段 0：基线与护栏

### 0.0 准备（不改业务逻辑）

- [x] **0.0.1** 确认 `mvn compile` 全模块通过
- [x] **0.0.2** 确认 `ai-quant` 主应用可启动（Tomcat 8118，47.5s）
- [x] **0.0.3** 创建分支 `refactor/backend-phase0`
- [x] **0.0.4** 记录 3～5 个关键回归用例（K 线、信号、下单、回测、登录）
  - **R1（K 线）**: 前端市场行情菜单加载 BTC/USDT 日线 → K 线图正常渲染，无服务端 5xx
  - **R2（信号）**: K 线图上技术信号（TechnicalSignal）与业务信号（TradeSignal）均显示为 markers
  - **R3（下单）**: 跟单/手动下单流程 → 订单成功写入 trade_order 表并返回 orderSn
  - **R4（回测）**: 创建回测任务 → 回测执行完成并产出结果报表
  - **R5（登录）**: 用户登录获取 token → 后续 API 携带 token 正常鉴权

### 0.1 统一 Java 与 MyBatis-Plus 版本

- [x] **0.1.1** 父 POM：`maven.compiler.release` 与 `compiler-plugin source/target` 统一为 **25**（团队选定一个）
- [x] **0.1.2** 各子模块 MyBatis-Plus 统一为 `mybatis-plus-spring-boot4-starter` **3.5.15**（与 ai-quant 一致）
- [x] **0.1.3** 全量编译 + 启动验证

**验收**：`dependency:tree` 中无 3.5.4 / 3.5.7 旧 starter（除明确排除项）。

### 0.2 清理 `bin/` 与仓库卫生

- [ ] **0.2.1** 根目录 `.gitignore` 增加：`**/bin/`、`**/.classpath`、`**/.project`
- [ ] **0.2.2** 从 Git 移除已跟踪的 `bin/` 目录（`git rm -r --cached` 各模块 bin）
- [ ] **0.2.3** 删除 `ai-quant/.tmp_bak_SSC.java`（若仍存在）
- [ ] **0.2.4** 从 `ai-quant/pom.xml` 删除重复的 `ai-engine` 依赖声明（保留一处）

**验收**：`git status` 无 bin 产物；搜索无 `.tmp_bak_`。

### 0.3 空模块与占位

- [ ] **0.3.1** 决策：`ai-account` 删除模块 vs 合并到 `ai-member`（建议：先从父 pom `<modules>` 注释并文档说明，下一迭代再删目录）
- [ ] **0.3.2** 更新 [Lenzito-后端重构计划.md](./Lenzito-后端重构计划.md) 或 README 中的模块列表

### 0.4 修正 ComponentScan

- [x] **0.4.1** `AiQuantApplication`：移除无效包 `com.chain.ai.trade.signal`、`com.chain.ai.trade.data`；补全 `engine.signal`、`engine.data`、`engine.xchange`、`backtest`、`extension` 等实际包（与 `AiTaskApplication` 对齐审查）
- [x] **0.4.2** 启动 ai-quant，确认 Bean 数量与关键 Controller 可访问（抽查 `/actuator` 或已知 health 接口）

### 0.5 依赖门禁（可选，推荐）

- [x] **0.5.1** 父 pom 或 ai-task 模块添加 Enforcer：`ai-task` 禁止依赖 `ai-quant`（**先只加 rule 文档化，下一小步再改 pom 断依赖**）
- [x] **0.5.2** 记录当前 `ai-task → ai-quant` 传递依赖原因（哪些 class 来自 quant）

### 0.6 文档对齐

- [ ] **0.6.1** 在 `完整架构设计文档.md` 顶部增加「实现状态」表：回测✅、实盘✅、Boot 4.0.1、**ai-ml 待建**
- [ ] **0.6.2** 更新根 `README.md`：生产入口 = **ai-quant** + **ai-task**（端口以配置为准）；删除 ai-order 独立启动说明

### 0.7 ai-order 去启动化（不做 Boot 应用）

- [x] **0.7.1** 删除 `ai-order/.../AiOrderApplication.java`
- [x] **0.7.2** 确认 `ai-order/pom.xml` **无** `spring-boot-maven-plugin`
- [x] **0.7.3** 更新 `ai-order/README.md`：标明「库模块，由 ai-quant 加载」
- [x] **0.7.4** 更新 `project-overview.bat` / 根 README：移除订单独立端口（如 8083）

**验收**：全仓库无第二套订单 `main()` 生产入口；`mvn -pl ai-order compile` 通过。

**阶段 0 完成标志**：编译通过、主应用启动、bin 已 ignore、Scan 已修正、order 无 Boot、文档已标注。

---

## 阶段 1：边界清晰化 + ai-ml 模块

### 1.0 新建 ai-ml（优先于大规模 Controller 迁移）

- [ ] **1.0.1** 父 `pom.xml` 增加 `<module>ai-ml</module>`（建议放在 `ai-data` 之后）
- [ ] **1.0.2** 创建 `ai-ml/pom.xml`：依赖 `ai-common`、`ai-data`、`smile-core`；**禁止** web、order、engine、quant
- [ ] **1.0.3** 父 `dependencyManagement` 增加 `ai-ml` 坐标
- [ ] **1.0.4** 迁入第一批（无 HTTP）：`service/ml/**`、`model/ml/**`、`mapper/Factor*`、`mapper/AutoSearch*`
- [ ] **1.0.5** `ai-quant` 添加 `ai-ml` 依赖，改 import；删除 quant 内已迁类
- [ ] **1.0.6** `MlProperties` / `quant.ml` 配置：保留在 quant 绑定或迁到 `ml.config`（二选一，文档记录）
- [ ] **1.0.7** `FactorMiningJobHandler`、`MLTrainingJobHandler` 归属 **ai-task** 或 **ai-ml/job**，`ai-task` 依赖 `ai-ml`
- [ ] **1.0.8** 回归：因子挖掘 API、模型训练 API、相关 XXL-JOB

**验收**：`ai-ml` 可独立 `mvn compile`；`ai-quant` 中无 `engine.service.ml` 包（或仅剩薄适配）。

---

## 阶段 1（续）：API 收敛与其它边界

> 细节步骤见 [../REFACTORING_PLAN.md](../REFACTORING_PLAN.md) 阶段 1.2～1.3（ApiResponse、GlobalExceptionHandler 等，部分可能已完成）。  
> **ML Controller**（`MlModelController` 等）过渡期可留在 ai-quant，只委托 ai-ml Service。

- [ ] **1.1** 库模块去掉 `spring-boot-starter-web`（从 ai-signal 等开始，逐模块验证）
- [ ] **1.2** 迁移 `TechnicalSignalController` → ai-quant
- [ ] **1.3** 迁移 member/logs/agent 的 Controller → ai-quant（或新建 `ai-api`）
- [ ] **1.4** 抽出 scheduler 模块，切断 `ai-task → ai-quant`
- [ ] **1.5** 定义 `SignServicePort` 等接口（ai-common 或各域 api 包）

---

## 阶段 2：God Class 拆分（建议顺序）

| 顺序 | 目标类 | 模块 | 状态 |
|------|--------|------|------|
| 2.1 | `DefaultSignService` | ai-signal | ⏳ |
| 2.2 | `MacdSignService` 等 support 类 | ai-signal | ⏳ |
| 2.3 | `TradeOrderServiceImpl` | ai-order | ⏳ |
| 2.4 | `DefaultDealStrategyTrade` | ai-engine | ⏳ |
| 2.5 | `BacktestEngine` / `BacktestService` | ai-engine | ✅ |
| 2.6 | `ElliottWaveEvaluator` | ai-risk | ⏳ |
| 2.7 | `BacktestController` | ai-quant | ⏳ |
| 2.8 | `LiveAdviceController` | ai-quant | ⏳ |
| 2.9 | `KLineV1ServiceImpl` | ai-quant | ⏳ |

每拆一类前：先补 1 个表征单测或集成测。

---

## 阶段 3～4

见 [Lenzito-后端重构计划.md](./Lenzito-后端重构计划.md) 第六、九节。

---

## 下一步（现在可做）

**从 0.0.1 或 0.7 开始**：在对话中说「开始阶段 0」或「先做 order 去启动化 / 先做 ai-ml 骨架」，我将按清单逐项改代码并每步汇报。

已完成的历史项（来自旧计划，勿重复做）：

- [x] `contants` → `constants` 包名修正（见 REFACTORING_PLAN.md 1.1）
