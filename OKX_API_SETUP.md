# OKX API配置指南

## 常见错误及解决方案

### 错误："50101:APIKey does not match current environment"

#### 问题描述
当测试API连接时出现 `50101:APIKey does not match current environment` 错误，表示API密钥与当前运行环境不匹配。

#### 根本原因
OKX提供测试环境和生产环境两种API环境：
- **测试环境**: 用于开发和测试
- **生产环境**: 用于实际交易

API密钥只能在创建时指定的环境中使用，不能跨环境使用。

#### 解决方案

##### 步骤1: 检查当前应用环境
确认您的应用是在哪个环境中运行：
- 如果是开发/测试环境，需要使用测试环境的API密钥
- 如果是生产环境，需要使用生产环境的API密钥

##### 步骤2: 登录OKX官网
1. 访问 [OKX官网](https://www.okx.com)
2. 登录您的账户

##### 步骤3: 创建正确的API密钥
1. 点击右上角头像 → "API" → "创建V5 API Key"
2. 根据您的应用环境选择：
   - **测试环境**: 勾选 "Testnet" 选项
   - **生产环境**: 不勾选 "Testnet" 选项（默认生产环境）
3. 设置API权限：
   - 交易权限：根据需要勾选读取/交易权限
   - IP白名单：建议设置IP限制提高安全性
4. 完成验证后创建API密钥

##### 步骤4: 记录API信息
创建成功后，您会得到：
- **API Key**: 公开密钥
- **Secret Key**: 私有密钥（仅显示一次，请妥善保存）
- **Passphrase**: API密码短语

##### 步骤5: 在应用中配置
在交易账户管理页面输入以上信息并保存。

#### 环境差异说明

| 环境 | 用途 | API地址 | 交易是否真实 |
|------|------|---------|-------------|
| 测试环境 | 开发测试 | testnet.okx.com | 否，使用测试币 |
| 生产环境 | 实际交易 | okx.com | 是，使用真实资金 |

#### 验证配置
1. 保存API配置后，点击"测试连接"
2. 如果显示"API连接测试成功"，说明配置正确
3. 如果仍有错误，请检查API密钥是否与环境匹配

## 其他常见问题

### 错误："API key not found"
- 检查API Key是否正确输入
- 确认API密钥没有过期或被删除

### 错误："Invalid signature"
- 检查Secret Key是否正确
- 确认Passphrase是否正确

### 错误："Permission denied"
- 检查API密钥权限设置
- 确认有足够的交易权限

### 网络连接问题
- 确保网络可以访问OKX服务器
- 配置代理（如果在中国大陆网络环境）

## 安全建议

1. **IP白名单**: 建议设置IP限制，只允许特定IP访问
2. **最小权限**: 只授予必要的API权限
3. **定期轮换**: 定期更换API密钥
4. **妥善保管**: Secret Key和Passphrase不要泄露给他人

## 获取帮助

如果问题仍然存在，请：
1. 检查OKX官方文档：https://www.okx.com/docs-v5/
2. 联系OKX客服
3. 查看应用日志获取更详细的错误信息





