<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.chain.ai.trade.engine.signal.mapper.SignalAlternateLogMapper">

    <select id="selectLastUnpaired" resultType="com.chain.ai.trade.engine.signal.entity.dos.SignalAlternateLog">
        SELECT * FROM signal_alternate_log
        WHERE strategy_name = #{strategyName}
          AND symbol = #{symbol}
          AND timeframe = #{timeframe}
          AND exit_signal_id IS NULL
        ORDER BY entry_time DESC
        LIMIT 1
    </select>

    <select id="selectAll" resultType="com.chain.ai.trade.engine.signal.entity.dos.SignalAlternateLog">
        SELECT * FROM signal_alternate_log
        WHERE strategy_name = #{strategyName}
          AND symbol = #{symbol}
          AND timeframe = #{timeframe}
        ORDER BY entry_time ASC
    </select>

</mapper>
